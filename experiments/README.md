# Experiments

Paper experiment commands live in:

```text
experiments/
```

This directory owns CGSReg paper experiment commands, run notes, summaries, and
artifact manifests. Backend-specific training commands may enter a backend
submodule for execution, but the paper-facing launch source lives here.
