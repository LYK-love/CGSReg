# Third-Party Backend Repositories

This directory is populated by:

```bash
bash scripts/bootstrap_repos.sh
```

The pinned repositories are listed in `third_party/backend_repos.lock`.

Do not commit large generated outputs, datasets, checkpoints, W&B logs, or
videos here.
