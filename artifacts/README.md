# Artifacts

This directory is for small manifests, checksums, and paper-facing metadata.

Do not commit large artifacts here. Store datasets, checkpoints, generated
videos, SAM2 masks, and W&B exports in the archive locations described by
`third_party/wm-evaluation/docs/`.
